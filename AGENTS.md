This is a repo for managing my guix system installs across a fleet of hosts at home.

It is supposed to be a composable project.

Everything on each host should be managed declaratively.

It should, depending on the host, automatically create the needed manifests, schemas, configs, and more, from a host definition.
