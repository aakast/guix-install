(define-module (system service-sets desktop)
  #:export (%desktop-services
            %desktop-packages
            %desktop-kernel-arguments))

(define %desktop-services
  '())

(define %desktop-packages
  '())

(define %desktop-kernel-arguments
  '())
